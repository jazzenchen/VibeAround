{
  "name": "@va/launcher",
  "private": true,
  "version": "0.7.15",
  "description": "Native terminal and desktop app launcher for VibeAround",
  "bin": {
    "va-launch": "bin/va-launch.mjs"
  },
  "scripts": {
    "build": "cargo build -p va-launcher",
    "test": "cargo test -p va-launcher"
  }
}
